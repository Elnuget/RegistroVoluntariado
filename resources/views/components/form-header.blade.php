<div class="form-header">
    <h1 class="form-title">{{ $title ?? 'Registro de Voluntariado' }}</h1>
    <p class="form-description">{{ $description ?? 'Complete el formulario para registrar su actividad de voluntariado' }}</p>
</div>
