<div class="form-header">
    <h1 class="form-title">{{ $title ?? 'Registro de Actividades' }}</h1>
    <p class="form-description">{{ $description ?? 'Complete el formulario para registrar su actividad' }}</p>
</div>
